class Customers:
    def __init__(self, accno, name, address, email, pnumber, balance, password, pid, bnumber):
        accno = self.accno
        name = self.name
        address = self.address 
        email = self.email
        phone_number = self.pnumber
        balance = self.balance
        password = self.password
        personal_id = self.pid
        branch_number = self.bnumber
